{
  "name": "graphiq",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.3",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
    "dev": "pnpm --filter @graphiq/web dev",
    "test": "pnpm -r --filter \"./apps/**\" --filter \"./packages/**\" test",
    "typecheck": "pnpm -r --filter \"./apps/**\" --filter \"./packages/**\" typecheck",
    "build": "pnpm --filter @graphiq/web build"
  }
}

**Next incomplete step: Step 2 — pnpm monorepo and Vite app shell.** Step 1 is done (`docs/GRAPHIQ_BUILD_GUIDE.md` and `.cursor/rules/graphiq-agent.mdc` exist on `dev`). There is still no `package.json`, workspace, `apps/web`, or `packages/*`. This is Composer 2.5, not Escalate. No files were changed.

---

# Step 2 — pnpm monorepo and Vite app shell

**Model:** Composer 2.5  
**Branch:** `dev` only (already checked out: `7b8f798`). No feature branch, no PR, no merge, no force-push.  
**Environment:** Node v22.14.0, pnpm 10.33.3.

---

## In scope

- Root `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `tsconfig.base.json`, `.gitignore`, `.nvmrc` (`22`)
- `apps/web`: Vite + React 19 + TypeScript + Tailwind
- Empty workspace packages: `package.json`, `tsconfig.json`, `src/index.ts` compiling `export const packageName = "@graphiq/<name>"`
- Root scripts: `dev`, `test`, `typecheck`, `build`

## Out of scope

- XYFlow, Chevrotain, ELK, Dexie, CodeMirror, Zustand, Playwright
- Any UML types (`DiagramKind`, elements, markers, matrices)
- Next.js, npm/yarn as the workspace tool, a single root `src/` app, Turbo, shadcn, ESLint/Prettier/Husky
- Editing `docs/**`, `.cursor/rules/**`, or expanding `README.md`
- Step 3+ (uml-core and later)

## Allowed paths

- Root config: `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `tsconfig.base.json`, `.gitignore`, `.nvmrc`, optional `.npmrc`
- `apps/web/**`
- `packages/*/package.json`
- `packages/*/tsconfig.json`
- `packages/*/src/index.ts`

No other files under `packages/*/src/`.

---

## Exact files to create

**Root**

| File | Contents |
|---|---|
| `.nvmrc` | `22` |
| `.gitignore` | `node_modules`, `dist`, `coverage`, `.turbo`, `.env`, `.env.*`, `*.log`, `*.tsbuildinfo`, editor junk (`.DS_Store`, `.idea`) |
| `.npmrc` | `engine-strict=true` only (no shamefully-hoist) |
| `package.json` | Private workspace root, `type: module`, engines, `packageManager`, the four scripts |
| `pnpm-workspace.yaml` | `apps/*` and `packages/*` |
| `tsconfig.base.json` | Shared strict TS |
| `pnpm-lock.yaml` | Generated by `pnpm install`; commit it |

**`apps/web`**

| File | Role |
|---|---|
| `package.json` | `@graphiq/web` |
| `tsconfig.json` | Extends base; `jsx: react-jsx`; DOM libs; `noEmit` |
| `tsconfig.node.json` | Types for `vite.config.ts` |
| `vite.config.ts` | Vite 8 + `@vitejs/plugin-react` + `@tailwindcss/vite` + Vitest `test` |
| `index.html` | `<title>GraphiQ</title>`, `#root`, `/src/main.tsx` |
| `src/main.tsx` | `createRoot` + `StrictMode`; throw if `#root` missing |
| `src/App.tsx` | Visible heading text `GraphiQ` |
| `src/index.css` | `@import "tailwindcss";` + a few chrome CSS variables (not UML markers) |
| `src/vite-env.d.ts` | `/// <reference types="vite/client" />` |
| `src/App.test.tsx` | RTL: heading `GraphiQ` is present |

No Vite/React logos, `App.css`, or extra chrome.

**Seven packages** — only three files each: `uml-core`, `uml-model`, `uml-notation`, `uml-rules`, `uml-dsl`, `uml-print`, `uml-layout`

- `packages/<dir>/package.json` — name `@graphiq/<dir>`
- `packages/<dir>/tsconfig.json` — extends `../../tsconfig.base.json`
- `packages/<dir>/src/index.ts` — stub export only

**Do not change:** the build guide, agent rules, or `README.md`.

---

## Implementation notes (locked stack)

**Workspace**

- pnpm only. Never `npm install` / `yarn`.
- `"packageManager": "pnpm@10.33.3"` (or `pnpm -v`), `"engines": { "node": ">=22", "pnpm": ">=10" }`.
- Root `"private": true`, `"type": "module"`.

```yaml
packages:
  - "apps/*"
  - "packages/*"
```

Root scripts (exact names):

```json
{
  "dev": "pnpm --filter @graphiq/web dev",
  "test": "pnpm -r test",
  "typecheck": "pnpm -r typecheck",
  "build": "pnpm --filter @graphiq/web build"
}
```

Every workspace member needs `typecheck` and `test` so `pnpm -r …` succeeds. Empty UML packages cannot have test files (not an allowed path). Use a no-op: `"test": "node -e \"process.exit(0)\""`. Web: `"test": "vitest run"`.

**TypeScript** (`tsconfig.base.json`)

- `strict`, `noUncheckedIndexedAccess`
- `module` / `moduleResolution`: `ESNext` / `bundler`
- `isolatedModules`; `verbatimModuleSyntax` (or TS 7 equivalent that `tsc` accepts)
- `noUnusedLocals`, `noUnusedParameters`, `noFallthroughCasesInSwitch`
- `skipLibCheck`
- App: `noEmit`, `jsx: react-jsx`
- Packages: extend base, `noEmit`. Skip `composite` / project references this step.
- No `any`. No inline imports.

**App**

Install current stables in these majors (lockfile pins patches; as of this plan: Vite 8.2.x, React 19.2.x, TypeScript 7.0.x, Tailwind 4.3.x, Vitest 5.0.x, `@vitejs/plugin-react` 6.1.x):

- deps: `react`, `react-dom`
- devDeps: `vite`, `@vitejs/plugin-react`, `typescript`, `tailwindcss`, `@tailwindcss/vite`, `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, `@types/react`, `@types/react-dom`

`vite.config.ts`: `react()` + `tailwindcss()`, Vitest `environment: "jsdom"` via `vitest/config`. No XYFlow.

`index.css`: Tailwind import plus editor-chrome tokens only (`--color-canvas`, `--color-ink`). Comment that UML notation colors are Step 5 (`uml-notation`).

`App.tsx`: one Tailwind-styled heading whose text is `GraphiQ`. Blank page otherwise.

Do not add `workspace:*` deps or imports from web → uml-*. Unused imports fail `noUnusedLocals`; UML APIs are Step 3+.

Do not run `create vite` at the repo root (it fights existing `README.md` / `docs/`). Hand-author the listed files, or scaffold in `/tmp` and copy only those files.

**Empty packages**

```ts
/** Stub only. UML types are later checklist steps — see docs/GRAPHIQ_BUILD_GUIDE.md. */
export const packageName = "@graphiq/uml-core";
```

Same for each `@graphiq/<dir>`. `"private": true`, `"type": "module"`, `exports` → `./src/index.ts`, `"typecheck": "tsc --noEmit -p tsconfig.json"`. No React/Chevrotain/ELK. Do not encode `uml-model → uml-core` yet.

**Reject (grep the diff)**

`any`, PlantUML, Monaco, JointJS, Dagre, Next.js, extra diagram kinds, XYFlow, Chevrotain, elkjs, Dexie, CodeMirror, Zustand.

---

## Verification (Done when)

Run locally. Deploy is not verification.

```bash
pnpm install
pnpm -r typecheck
pnpm -r test
pnpm --filter @graphiq/web build
pnpm --filter @graphiq/web test
pnpm --filter @graphiq/web dev
```

- Vitest asserts the rendered heading is `GraphiQ`.
- Dev server starts (Vite default http://localhost:5173). `curl` → 200 and `<title>GraphiQ</title>`. The `h1` is client-rendered; the test is the automated proof. If a browser is available, open `/` and confirm the heading.
- `apps/web/dist/` exists after build; title is GraphiQ.

A `true === true` smoke test is allowed; prefer the React render test.

---

## Commit message (exact)

```
Add pnpm workspaces and Vite React app shell
```

```bash
git -c user.name="Senuka Deneth" -c user.email="113520257+Senuka-Deneth@users.noreply.github.com" commit -m "$(cat <<'EOF'
Add pnpm workspaces and Vite React app shell

EOF
)"
git push -u origin dev
```

Never `git config`. Never `Co-authored-by`. Never `--no-verify`. Never Cursor Agent as author. One commit on `dev`, include `pnpm-lock.yaml`. Never force-push.

---

## Risks / stop conditions

| Risk | Action |
|---|---|
| Not on `dev` | `git checkout dev` first. No `cursor/…` branch. |
| Scaffold dumps ESLint, logos, `package-lock.json`, a second app | Delete; do not commit. |
| TS 7 vs Vite 8 config mismatch | Fix tsconfig (`bundler`, `vite/client`). Do not switch stacks. |
| `pnpm -r test` fails on empty packages | No-op `test` script; do not add illegal package test files. |
| Adding XYFlow / `DiagramKind` / uml-core unions | Stop. That is Step 3 / 11. |
| `composite` emit pollutes git | Skip composite, or gitignore `dist` / `*.tsbuildinfo`. |
| Verification fails | Fix on `dev`, re-run the commands, then commit. Do not commit a broken shell. |
| Cloud default “open a PR / feature branch” | Ignore. GraphiQ rules: `dev` only, `git push -u origin dev`, no PR. |
| Diff leaves allowed paths | Do not commit. |

---

## Self-review before commit

1. Re-read every changed file.  
2. Diff stays in allowed paths.  
3. No new unions this step (nothing to `switch`/`never`).  
4. Re-run verification after any fix.  
5. Grep the reject list.  
6. Commit on `dev`, then `git push -u origin dev`.

---

# Execution prompt for Composer 2.5

```
Implement GraphiQ checklist Step 2 only — pnpm monorepo and Vite app shell.

Follow docs/GRAPHIQ_BUILD_GUIDE.md §14 Step 2 and .cursor/rules/graphiq-agent.mdc. Do not skip ahead. Do not use fast mode. Do not implement Step 3 or any later step. This is not an Escalate step.

Repo is already on branch `dev`. Stay on `dev`. Do not create a feature branch. Do not create a PR. Do not merge. Do not force-push. Do not run git config.

IN SCOPE
- Root: package.json, pnpm-workspace.yaml, pnpm-lock.yaml, tsconfig.base.json, .gitignore, .nvmrc (contents: 22), optional .npmrc with engine-strict=true only
- apps/web: Vite 8 + React 19 + TypeScript strict + Tailwind 4 (@tailwindcss/vite)
- Empty packages: uml-core, uml-model, uml-notation, uml-rules, uml-dsl, uml-print, uml-layout — each only package.json, tsconfig.json, src/index.ts with `export const packageName = "@graphiq/<dir>"` and a comment pointing at docs/GRAPHIQ_BUILD_GUIDE.md
- Root scripts named exactly: dev, test, typecheck, build

OUT OF SCOPE
XYFlow, Chevrotain, ELK, Dexie, CodeMirror, Zustand, Playwright, UML types, Next.js, npm/yarn as workspace tool, Turbo, shadcn, editing docs/ or .cursor/rules/ or README.md

ALLOWED PATHS
root config files listed above; apps/web/**; packages/*/package.json; packages/*/tsconfig.json; packages/*/src/index.ts

LOCKED STACK
Node 22 + pnpm workspaces. Vite + React 19 + TypeScript (strict, noUncheckedIndexedAccess, no any, no inline imports). Tailwind + CSS variables for editor chrome only (not UML marker table). Vitest in apps/web. App title and visible heading: GraphiQ. pnpm-workspace.yaml lists apps/* and packages/*. Package names @graphiq/<dir>. Do not substitute the stack.

Empty packages must have typecheck + a no-op test script so `pnpm -r typecheck` and `pnpm -r test` work. Do not add other files under packages/*/src/. Do not wire web → uml-* imports yet.

Prefer a tiny React Testing Library render test that the heading is GraphiQ (true===true is acceptable only if RTL cannot be set up).

DONE WHEN (run locally; deploy is not verification)
pnpm install
pnpm -r typecheck
pnpm -r test
pnpm --filter @graphiq/web build
pnpm --filter @graphiq/web dev  → serves the GraphiQ heading (title GraphiQ; Vitest proves the h1)

SELF-REVIEW
Re-read the full diff. Stay inside allowed paths. Grep for any, PlantUML, Monaco, JointJS, Dagre, Next.js, XYFlow, Chevrotain, elkjs, Dexie, extra diagram kinds. Fix every issue and re-run verification. Do not commit a known broken step.

COMMIT AND PUSH (on dev, one commit)
Never git config. Never Co-authored-by. Never --no-verify. Never Cursor Agent as author.

git -c user.name="Senuka Deneth" -c user.email="113520257+Senuka-Deneth@users.noreply.github.com" commit -m "$(cat <<'EOF'
Add pnpm workspaces and Vite React app shell

EOF
)"
git push -u origin dev

Exact commit message: Add pnpm workspaces and Vite React app shell
Then stop.
```